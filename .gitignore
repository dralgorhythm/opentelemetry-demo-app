/target

# Claude Code harness artifacts (machine-local, never repo content)
.claude/

# Terraform working dirs and state — state lives in S3, providers are
# re-fetched by `terraform init` (only .terraform.lock.hcl is tracked).
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
